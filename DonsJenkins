pipeline {
    agent any
    parameters {
    string(name: 'Branch_name', defaultValue: 'main', description: 'enter to build')}
       stages {
        stage('Hello') {
            steps {
                echo 'Hello'
            }
        }
        stage('Git Clone') {
            steps {
                echo 'Cloning from GitHub'
                git branch: 'main', changelog: false, credentialsId: 'For-github', poll: false, url: 'https://github.com/IBT-learning/DevOps-Git/'
            }
        }
        stage('Git Verify') {
            steps {
                bat 'dir'
            }
        }
          
          stage('Adding stage from DonsJenkins') {
            steps {
                echo 'Updated from github'
            }
         }
   }
}
